# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit real keys
.env
.env.*
!.env.example

# Local design-handoff snapshot (duplicated CSS for sharing with Claude Design)
design-handoff/

# Design redesign drop / temp extraction (the theme itself lives in src/)
_design_unzip/
*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
